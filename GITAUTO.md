## TypeScript
- Test TypeScript configurations (`tsconfig.test.json`) must explicitly define `include` patterns for test files and `exclude` patterns for `node_modules` to prevent relaxing strict type checks on production source code.
